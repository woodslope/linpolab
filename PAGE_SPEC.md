# 首页页面规格

- 页面角色：LINPO LAB 的公开入口与内容聚合页。
- 内容范围：精选项目、AI 设计工作流、Vibe Coding 实验、个人介绍与联系方式。
- 聚合边界：首页链接已注册且公开的仓库内容包，以及已声明的独立 H5 归档；各内容的项目样式、页面文案和运行实现仍由对应产品负责。
- 公开路由：`index.html`。
- 视觉实现：首页使用 CSS 与原生浏览器 API 实现分层动效：Hero 叙事入场、区块错峰显现、卡片媒体交互与导航区块反馈；不依赖第三方动画运行时。
- 媒体策略：Hero 优先加载；卡片媒体按接近视口时再请求，并在图片解码后淡入。首页部署图位于 `assets/home-media/`，可替换源图位于仓库外层 `resources/source-images/linpolab/home-generated/`。
