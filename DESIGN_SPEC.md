# LINPO LAB Home Design Spec

本文档提炼自 `index.html` 当前首页，用于后续维护首页视觉、文案和响应式规则。它不是全站品牌手册，优先约束 `linpo-lab-home-v1` 首页。

## 1. 设计定位

- 页面类型：个人设计作品集首页。
- 核心叙事：Design x Method x AI。
- 视觉气质：克制、研究型、黑白为主、带少量科技感高光。
- 内容主线：品牌体验、产品设计、设计方法、AI 工作流。

## 2. 颜色规范

### 主背景

- `--paper: #f4f4f1`：首页主体浅色纸面背景。
- `--paper-2: #fafaf8`：备用浅色背景。
- Hero / 深色卡片背景：接近黑色，主要使用 `#040404`、`#080808`、`#090909`、`#06070a`。

### 文字颜色

- 深色页面主文字：`#050505`、`#1b1b1d`。
- 深色页面辅助文字：`#414346`、`#6d6f73`。
- 黑底主文字：`#ffffff` 或 `rgba(255,255,255,0.86-0.95)`。
- 黑底说明文字：`rgba(255,255,255,0.52-0.84)`。

### 强调色

- `--blue: #4078ff`
- `--violet: #8c6df3`
- `--gold: #e2a733`

使用原则：
- 首页不做大面积彩色背景。
- 强调色主要出现在图片资产、焦点态或少量图像高光里。
- 不新增高饱和渐变、霓虹按钮或彩色装饰块。

## 3. 字体与字重

### 字体

- 主字体：Inter + 系统无衬线 + 中文系统字体。
- 不混用衬线字体。
- 英文用于辅助解释，不承担主要阅读负担。

### 字重档位

统一使用整档字重和 CSS token，避免 `520`、`640`、`780` 这类零散值。

| Token | 字重 | 用途 |
| --- | --- | --- |
| `--font-weight-light` | 300 | 数字索引、低强调装饰信息 |
| `--font-weight-regular` | 400 | 普通正文、弱信息 |
| `--font-weight-medium` | 500 | 说明文字、英文辅助、元信息 |
| `--font-weight-semibold` | 600 | 导航、信息标签 |
| `--font-weight-bold` | 700 | 区块标题、重要中文标题 |
| `--font-weight-heavy` | 800 | Hero 标题、品牌主标题、卡片标题 |

当前应用：
- Hero 中文说明：`500`
- About 右栏中文值：`600`
- About 右栏英文辅助：`500`
- Section 中文标题：`--font-weight-bold`
- Hero H1：`--font-weight-heavy`

## 4. 字号层级

当前首页以 CSS 变量管理字号：

- Hero H1：`clamp(58px, 7.4vw, 88px)`
- Hero 英文轴线：`clamp(28px, 3.1vw, 34px)`
- Hero 中文说明：`16px`
- Section 编号：`26px`
- Section 中文标题：`18px`
- 卡片标题：`15-19px`
- 正文和辅助信息：`10-14px`
- 极小英文辅助：`9-10px`

使用原则：
- Hero 是唯一允许超大字号的位置。
- 卡片、侧栏、About 信息组不用大标题字号。
- 英文辅助小于中文主读信息。

## 5. 圆角与边框

### 圆角

- 默认卡片圆角：`7px`
- 大图圆角：`12px`
- 小元素圆角：`5-6px`
- 头像：圆形或 pill。

使用原则：
- 卡片圆角保持克制，不做大圆角卡片。
- 不混用大量不同圆角风格。

### 线条

- 主分隔线：`rgba(0,0,0,0.13)`
- 弱分隔线：`rgba(0,0,0,0.08)`
- 黑底线条应低透明度，避免抢内容。
- 纯装饰竖线慎用，只有能明确组织信息时才保留。

## 6. 间距与布局

### 容器

- 最大宽度：`1240px`
- 桌面左右内边距：`46px`
- 平板和移动端左右内边距：`22px`

### Section

- 桌面为左侧栏 + 右内容区。
- 左侧栏宽度：`170px`
- 栏间距：`44px`
- `900-721px` 区间 section 侧栏改为稳定两列：左侧编号和标题固定约 `160px`，右侧说明垂直居中。
- `720px` 以下 section 侧栏改为单列，侧栏在上。

### 卡片间距

- 卡片间距：`10-12px`
- 卡片内部左右 padding：常规 `30px`，紧凑卡片 `18px`

使用原则：
- 首页整体偏紧凑，避免大面积空白造成内容稀薄。
- 黑底图片卡片依靠图片和文字层级区分，不额外套卡片。

## 7. 响应式断点

当前主要断点：

- `1180px`：方法论和实验卡片改为两列，避免深色图片卡片在窄桌面被压得过小；About 内部从三列调整为“左侧照片跨行 + 右侧简介和轻信息”。
- `900px`：Hero 改为单列，section 改为上下结构，方法和实验卡片改为两列。
- `760px`：About 轻信息改为单列，避免邮箱在窄两列里挤压。
- `720px`：section 侧栏说明改为上下堆叠，并收紧标题与说明之间的间距。
- `620px`：移动端布局，卡片单列，Hero 图片和 About 卡片进一步收紧。

响应式原则：
- 先避免横向溢出，再追求紧凑。
- `720-900px` 是重点检查区间，不要只看桌面和手机。
- `620px` 以下 Hero 文字、头像信息和主视觉之间要保留明确呼吸间距，避免头像信息贴住主视觉。
- `620px` 以下 section 标题说明到内容区的距离应略紧于平板端，避免每个区块都像桌面侧栏直接堆叠。
- About 信息组在窄屏保持清晰，不让英文挤压中文。

## 8. 组件规范

### Hero

- 结构：品牌导航 + 左侧文字 + 右侧主视觉。
- Hero 中文说明是说明文，不能比主标题更抢。
- 右侧图像可以大，但不能造成横向溢出或遮挡。

### Section 侧栏

- 结构：编号 + 中文标题 + 英文标题 + 一句说明。
- 中文标题使用 `h2`，section 使用 `aria-labelledby` 关联对应标题。
- 说明格式建议为：英文短标签 + 中文解释。
- 中文解释应围绕首页主线，不扩写为项目详情。
- 平板两列状态下，右侧说明起点应统一，不跟随左侧标题宽度漂移。

### 项目卡片

- 项目卡片以大横幅为主。
- 可点击卡片必须有 hover 和 focus 状态。
- 项目卡片标题使用 `h3`，不要越过 section 标题层级。
- 未开放项目不应表现得像强 CTA。

### 方法论与实验卡片

- 使用同一套黑底图片卡片语言。
- 标题字号、字重、行高保持一致。
- 英文副标题为辅助信息，不抢中文标题。
- 两类卡片共用同一套左上角遮罩，保证标题和英文副标题在亮部图片上仍可读。
- 黑底图片卡片可以使用极弱内边线，但不能额外套卡片或加入明显发光边框。
- `1180px` 以下卡片改为两列，优先保证英文副标题自然换行和图片可读面积。

### About

- About 是信任和能力确认区，不是第二个 Hero。
- 中文信息优先，英文辅助说明。
- 能力方向放在中间简介里，不放进右侧信息栏。
- 右侧信息栏只保留工作地点、联系合作这类轻量信息。
- 右侧信息统一使用“标题 + 中文值 + 英文辅助”的结构，不做表格、小卡片或 2 x 2 模块。

## 9. 图片资产规范

- `assets/generated/` 保留原始生成图，不直接覆盖。
- `assets/optimized/` 存放首页实际加载的优化图。
- 首页 CSS 背景图和 Hero 主图优先引用 `assets/optimized/`。
- 无透明通道的生成图优先转为 JPG，控制在对应展示尺寸的 2x 左右。
- favicon 保留独立小尺寸 PNG。
- Hero 主图当前使用 `1100 x 1100`，用于桌面约 `500px` 展示和移动端高倍屏。
- About 头像图当前使用 `760 x 760`。
- 方法论卡片当前使用 `1000px` 宽图。
- 实验卡片当前使用 `1100px` 宽图。
- 项目横幅当前使用 `1400px` 宽图。
- 新增图片时先保留原图，再生成首页尺寸优化图，并复查图片是否丢失、裁切是否合理、页面是否出现横向滚动。

## 10. 交互规范

- 导航链接：hover 和 focus 时显示细线反馈。
- 可点击项目卡片：hover 时背景轻微放大，focus 有清晰轮廓。
- 邮箱：使用 `mailto:`，hover 和 focus 时出现下划线。
- 品牌链接、导航、邮箱、返回顶部都要有可见 `focus-visible` 状态。
- 文字较短的链接要通过 padding 保证可点击区域，不只依赖文字本身大小。
- 不添加复杂动效，除非能明确服务层级、反馈或叙事。

## 11. 文案规范

- 首页文案围绕四个关键词：品牌体验、产品设计、设计方法、AI 工作流。
- 中文负责主阅读，英文负责辅助定位。
- 不使用斜杠串联长英文能力词。
- 不在首页扩写项目详情。
- 不使用过度营销词，比如“颠覆”“赋能”“全新升级”等空泛表达。

## 12. QA 检查清单

每次修改首页后至少检查：

- `1280px`
- `1024px`
- `900px`
- `768px`
- `721px`
- `720px`
- `620px`
- `390px`

检查项：
- 页面无横向滚动。
- 文字无溢出。
- Hero 首屏不拥挤。
- About 信息组不挤压。
- 卡片标题和英文副标题不重叠。
- 图片无 404。
- 控制台无明显错误。
- 项目卡片链接和邮箱链接存在。
